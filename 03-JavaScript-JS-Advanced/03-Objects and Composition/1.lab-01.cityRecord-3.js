function cityRecord(name, population, treasury) {    // 100/100

    return {
        name,
        population,
        treasury,
    };
}

console.log(cityRecord('Tortuga', 7000, 15000));
console.log('-------');
console.log(cityRecord('Santo Domingo', 12000, 23500));