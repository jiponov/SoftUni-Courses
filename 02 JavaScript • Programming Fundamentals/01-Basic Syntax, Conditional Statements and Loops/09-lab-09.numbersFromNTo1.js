function numbersFromNTo1(num) {

    while (num >= 1) {
        console.log(num);
        num--;
    }
}
numbersFromNTo1(5);
numbersFromNTo1(3);

// HINT: Create a while loop with condition N >= 1. Print N and decrease it with each step.